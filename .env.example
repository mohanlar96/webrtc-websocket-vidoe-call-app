## Copy to `.env` and adjust values.
## Note: Vite only exposes env vars prefixed with `VITE_` to the browser.

# WebSocket signaling server URL used by `getSignalingUrl()` in `src/App.jsx`
VITE_SIGNALING_URL=ws://localhost:9001

